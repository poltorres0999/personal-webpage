import React from 'react';
import { Container, Typography } from '@mui/material';

const Experience: React.FC = () => {
  return (
    <Container id="experience" sx={{ my: 4 }}>
      <Typography variant="h2" sx={{ mb: 2 }}>
        Experience
      </Typography>
      {/* Add your content here */}
    </Container>
  );
};

export default Experience;